// JavaScript Program to Empty an Array

var arr = [1,2,4,5,6];


var b = arr;

arr = []; // as you can see when we resign the array is became empty and b will not change

console.log(b);

console.log(arr);