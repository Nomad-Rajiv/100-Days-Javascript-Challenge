// JavaScript Program to Set a Default Parameter Value For a Function\

const mult = (a,b=4) =>{

  console.log(`The multiply of two number is ${a*b}`);
}

mult(4)

// Here we are passing only one parameter in the fucntion and in argument side we pass only one argument

// here b = 4 we assigned the value of b in the argument that is called default argument 